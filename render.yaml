services:
  - type: web
    name: epineon-ai-engine
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python run.py --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
